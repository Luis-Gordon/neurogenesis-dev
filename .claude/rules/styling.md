---
globs: "src/styles/**,**/*.css,**/*.astro,**/*.tsx,**/*.jsx"
---

- Use the project's CSS framework — no inline styles or CSS-in-JS
- CSS custom properties for design tokens (colors, spacing, typography)
- Mobile-first responsive design — start with base styles, add breakpoints
- Self-hosted fonts only — no external CDN requests (Google Fonts, Adobe)
- Dark mode support via CSS custom properties and class toggling
- Prefer CSS transitions over JavaScript animations for simple effects
- Max prose width ~65ch for readability
